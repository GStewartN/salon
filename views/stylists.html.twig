<!DOCTYPE html>
<html>
  <head>
    <title>Hacks Hair Hole</title>
  </head>
  <body>
    <h1>Stylists</h1>

    {% if stylists is not empty %}
      <p>Here are your stylists:</p>
      <ul>
        {% for stylist in stylists %}
          <li>{{ stylist.getName }}</li>
        {% endfor %}
      </ul>
    {% endif %}

    <form action="/stylists" name="style" method="post">
      <label for="name">Stylist Name</label>
      <input id="name" name="name" type="text" required>

      <button name="style" type="submit">Add Stylist</button>
    </form>

    <p><a href='/'>Home</a></p>
  </body>
</html>
